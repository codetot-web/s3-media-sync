{
  "name": "s3-media-sync/plugin",
  "description": "WordPress plugin to sync media to S3-compatible storage",
  "type": "wordpress-plugin",
  "require": {
    "php": ">=7.4",
    "aws/aws-sdk-php": "^3.0"
  },
  "autoload": {
    "psr-4": {
      "S3MediaSync\\": "includes/"
    }
  }
}
